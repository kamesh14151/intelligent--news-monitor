# Member 1 — Semantic Discovery

**Owner:** Kamesh

**Branch:** `feature/agent-discovery`

**Main file:** `backend/app/agents/discovery.py`

Start here:
1. Read `docs/agent-development-guide.md`.
2. Read `team/TEAM_TASKS.md` → Member 1.
3. Run the existing tests with `pytest -q`.
4. Test the agent with the sample fixture in `tests/fixtures/sample_news.json`.
5. Improve semantic relevance without changing the shared state/schema.

Goal: `raw_articles → relevant_articles`.
