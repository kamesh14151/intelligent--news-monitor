# Member 2 — Context Validation

**Branch:** `feature/agent-validation`

**Main file:** `backend/app/agents/validation.py`

Start here:
1. Read `docs/agent-development-guide.md`.
2. Read `team/TEAM_TASKS.md` → Member 2.
3. Understand `ValidationResult` in `backend/app/schemas/news.py`.
4. Keep structured LLM output and deterministic mock behavior.
5. Add tests for true/false relevance.

Goal: `relevant_articles → validated_articles`.
