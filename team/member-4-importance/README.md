# Member 4 — Importance Analysis

**Branch:** `feature/agent-importance`

**Main file:** `backend/app/agents/importance.py`

**Supporting schema:** `ImpactResult` in `backend/app/schemas/news.py`.

Start here:
1. Read `docs/agent-development-guide.md`.
2. Read `team/TEAM_TASKS.md` → Member 4.
3. Keep scores in the documented ranges.
4. Use structured LLM output for the reasoning signals.
5. Do not move priority thresholds into the LLM.

Goal: `stories → impact signals → scored_stories`.
